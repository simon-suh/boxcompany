apiVersion: apps/v1
kind: Deployment
metadata:
  name: jenkins
  namespace: jenkins
  labels:
    app: jenkins
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jenkins
  template:
    metadata:
      labels:
        app: jenkins
    spec:
      serviceAccountName: jenkins
      securityContext:
        fsGroup: 1000
        runAsUser: 1000

      # ── Init container: installs plugins before Jenkins starts ──────────────
      initContainers:
        - name: install-plugins
          image: jenkins/jenkins:lts
          command:
            - jenkins-plugin-cli
            - --plugin-file=/plugins/plugins.txt
            - --plugin-download-directory=/var/jenkins_home/plugins
          env:
            - name: JAVA_OPTS
              value: "-Xmx512m"
          volumeMounts:
            - name: jenkins-home
              mountPath: /var/jenkins_home
            - name: plugins
              mountPath: /plugins
          resources:
            requests:
              memory: "512Mi"
              cpu: "100m"
            limits:
              memory: "1Gi"
              cpu: "500m"

      containers:
        - name: jenkins
          image: jenkins/jenkins:lts
          ports:
            - name: http
              containerPort: 8080
            - name: agent
              containerPort: 50000

          env:
            - name: JAVA_OPTS
              value: "-Djenkins.install.runSetupWizard=false"
            - name: CASC_JENKINS_CONFIG
              value: /var/jenkins_casc/jenkins.yaml
            - name: JENKINS_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: jenkins-credentials
                  key: admin-password
            - name: JENKINS_DEV_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: jenkins-credentials
                  key: dev-password
            - name: GITHUB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: jenkins-credentials
                  key: github-username
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: jenkins-credentials
                  key: github-token

          volumeMounts:
            - name: jenkins-home
              mountPath: /var/jenkins_home
            - name: casc
              mountPath: /var/jenkins_casc

          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "2Gi"
              cpu: "1500m"

          readinessProbe:
            httpGet:
              path: /login
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 15

          livenessProbe:
            httpGet:
              path: /login
              port: 8080
            initialDelaySeconds: 120
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 5

      volumes:
        - name: jenkins-home
          persistentVolumeClaim:
            claimName: jenkins-home
        - name: casc
          configMap:
            name: jenkins-casc
        - name: plugins
          configMap:
            name: jenkins-plugins
